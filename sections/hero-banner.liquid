{%- liquid
  assign slide_blocks = section.blocks | where: 'type', 'slide'
  assign has_carousel_slides = false
  for block in slide_blocks
    if block.settings.image != blank
      assign has_carousel_slides = true
      break
    endif
  endfor
-%}

<section
  class="hero-banner{% if has_carousel_slides %} hero-banner--carousel{% endif %}"
  id="hero-banner-{{ section.id }}"
  role="region"
  aria-label="{{ section.settings.heading | default: 'Hero' }}"
  {% if has_carousel_slides %}
    data-autoplay-speed="{{ section.settings.carousel_speed | times: 1000 }}"
    data-transition-duration="{{ section.settings.transition_duration }}"
    data-paused="false"
  {% endif %}
>
  <div class="hero-banner__image">
    {% if has_carousel_slides %}
      {%- assign first_image_done = false -%}
      {%- for block in slide_blocks -%}
        {%- assign has_image = false -%}
        {%- if block.settings.image != blank -%}{%- assign has_image = true -%}{%- endif -%}
        <div
          class="hero-banner__slide{% unless has_image %} hero-banner__slide--empty{% endunless %}{% if has_image %}{% unless first_image_done %} hero-banner__slide--active{% assign first_image_done = true %}{% endunless %}{% endif %}"
          data-slide-index="{{ forloop.index0 }}"
          {{ block.shopify_attributes }}
        >
          {%- if block.settings.image != blank -%}
            <img
              src="{{ block.settings.image | image_url: width: 1920 }}"
              srcset="
                {{ block.settings.image | image_url: width: 960 }} 960w,
                {{ block.settings.image | image_url: width: 1280 }} 1280w,
                {{ block.settings.image | image_url: width: 1920 }} 1920w,
                {{ block.settings.image | image_url: width: 2560 }} 2560w
              "
              sizes="100vw"
              alt="{{ block.settings.image.alt | default: '' }}"
              width="{{ block.settings.image.width }}"
              height="{{ block.settings.image.height }}"
              loading="{% if forloop.first %}eager{% else %}lazy{% endif %}"
              fetchpriority="{% if forloop.first %}high{% else %}low{% endif %}"
            >
          {%- else -%}
            <span class="hero-banner__slide-placeholder">Select image in Theme Customizer → click this slide → Slide image</span>
          {%- endif -%}
        </div>
      {%- endfor -%}
    {% else %}
      {% if section.settings.hero_image %}
        <div class="hero-banner__slide hero-banner__slide--active">
          <img
            src="{{ section.settings.hero_image | image_url: width: 1920 }}"
            alt="{{ section.settings.hero_image.alt | default: section.settings.heading }}"
            width="{{ section.settings.hero_image.width }}"
            height="{{ section.settings.hero_image.height }}"
            loading="eager"
          >
        </div>
      {% else %}
        <div class="hero-banner__slide hero-banner__slide--active">
          <img
            src="https://images.unsplash.com/photo-1542838132-92c53300491e?w=1920&q=80"
            alt="Fresh natural fruits and vegetables"
            loading="eager"
          >
        </div>
      {% endif %}
    {% endif %}
    <div
      class="hero-banner__overlay"
      style="--overlay-opacity: {{ section.settings.overlay_opacity | default: 85 | divided_by: 100.0 }};"
    ></div>
    <div class="hero-banner__gradient-ornament"></div>
  </div>
  
  <div class="hero-banner__content">
    <div class="hero-banner__text">
      {% if section.settings.heading != blank %}
        <h1 class="hero-banner__heading">
          <span class="hero-banner__heading-line">{{ section.settings.heading }}</span>
        </h1>
      {% endif %}
      
      {% if section.settings.subheading != blank %}
        <p class="hero-banner__subheading">{{ section.settings.subheading }}</p>
      {% endif %}

      <div class="hero-banner__trust-badges">
        <span class="trust-badge">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M10 2L12 7L17 7.5L13.5 11L14.5 16L10 13.5L5.5 16L6.5 11L3 7.5L8 7L10 2Z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          Premium Quality
        </span>
        <span class="trust-badge">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M10 18C14.4183 18 18 14.4183 18 10C18 5.58172 14.4183 2 10 2C5.58172 2 2 5.58172 2 10C2 14.4183 5.58172 18 10 18Z" stroke="currentColor" stroke-width="1.5"/>
            <path d="M7 10L9 12L13 8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          100% Natural
        </span>
        <span class="trust-badge">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M10 2L12 7L17 7.5L13.5 11L14.5 16L10 13.5L5.5 16L6.5 11L3 7.5L8 7L10 2Z" fill="currentColor"/>
          </svg>
          Lab Tested
        </span>
      </div>
      
      <div class="hero-banner__ctas">
        {% if section.settings.cta_text != blank and section.settings.cta_link != blank %}
          <a href="{{ section.settings.cta_link }}" class="btn btn-primary btn-large hero-banner__cta-primary">
            <span>{{ section.settings.cta_text }}</span>
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M7.5 15L12.5 10L7.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
        {% endif %}
        
        {% if section.settings.secondary_cta_text != blank and section.settings.secondary_cta_link != blank %}
          <a href="{{ section.settings.secondary_cta_link }}" class="btn btn-outline btn-large hero-banner__cta-secondary">
            {{ section.settings.secondary_cta_text }}
          </a>
        {% endif %}
      </div>
    </div>
    
    <div class="hero-banner__scroll-indicator">
      <span>Scroll</span>
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M7 10L12 15L17 10" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </div>

    {%- if has_carousel_slides and slide_blocks.size > 1 -%}
      <div class="hero-banner__carousel-controls" role="group" aria-label="Carousel controls">
        <button
          type="button"
          class="hero-banner__carousel-prev"
          aria-label="Previous slide"
          data-action="prev"
        >
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M15 18L9 12L15 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
        <button
          type="button"
          class="hero-banner__carousel-next"
          aria-label="Next slide"
          data-action="next"
        >
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M9 18L15 12L9 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
        <div class="hero-banner__carousel-dots" role="tablist" aria-label="Slide navigation">
          {%- for block in slide_blocks -%}
            {%- if block.settings.image != blank -%}
              <button
                type="button"
                class="hero-banner__carousel-dot{% if forloop.first %} hero-banner__carousel-dot--active{% endif %}"
                role="tab"
                aria-selected="{% if forloop.first %}true{% else %}false{% endif %}"
                aria-label="Slide {{ forloop.index }}"
                data-slide-index="{{ forloop.index0 }}"
              ></button>
            {%- endif -%}
          {%- endfor -%}
        </div>
      </div>
    {%- endif -%}
  </div>
</section>

{% stylesheet %}
  .hero-banner {
    position: relative;
    width: 100%;
    max-width: 100%;
    height: 90vh;
    min-height: 600px;
    max-height: 900px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  .hero-banner__image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
    overflow: hidden;
  }

  .hero-banner__slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    transition: opacity var(--hero-transition-duration, 800ms) ease-in-out;
    pointer-events: none;
  }

  .hero-banner__slide--active {
    opacity: 1;
    z-index: 1;
  }

  .hero-banner__slide--empty {
    display: none;
  }

  .hero-banner__slide--empty .hero-banner__slide-placeholder {
    display: none;
  }

  .hero-banner__image img,
  .hero-banner__slide img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .hero-banner__overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      135deg,
      rgba(45, 80, 22, 0.9) 0%,
      rgba(45, 80, 22, 0.7) 40%,
      rgba(74, 124, 44, 0.5) 100%
    );
    opacity: var(--overlay-opacity, 1);
    z-index: 2;
    overflow: hidden;
  }

  .hero-banner__overlay::before {
    content: '';
    position: absolute;
    top: -20%;
    right: -5%;
    width: 400px;
    height: 400px;
    max-width: 60vw;
    max-height: 60vh;
    background: radial-gradient(circle, rgba(74, 124, 44, 0.2) 0%, transparent 70%);
    border-radius: 50%;
    animation: float 6s ease-in-out infinite;
  }

  .hero-banner__overlay::after {
    content: '';
    position: absolute;
    bottom: -15%;
    left: -3%;
    width: 350px;
    height: 350px;
    max-width: 55vw;
    max-height: 55vh;
    background: radial-gradient(circle, rgba(74, 124, 44, 0.2) 0%, transparent 70%);
    border-radius: 50%;
    animation: float 8s ease-in-out infinite reverse;
  }

  .hero-banner__gradient-ornament {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 150px;
    max-height: 20vh;
    background: linear-gradient(
      180deg,
      transparent 0%,
      rgba(74, 124, 44, 0.12) 100%
    );
    z-index: 2;
    clip-path: polygon(0 50%, 100% 0%, 100% 100%, 0% 100%);
    overflow: hidden;
  }

  .hero-banner__content {
    position: relative;
    z-index: 3;
    width: 100%;
    max-width: 1400px;
    padding: var(--spacing-xl) var(--spacing-lg);
    margin: 0 auto;
    text-align: left;
    display: flex;
    flex-direction: column;
    justify-content: center;
    min-height: 100%;
    animation: fadeInUp 1s cubic-bezier(0.16, 1, 0.3, 1);
    box-sizing: border-box;
  }

  .hero-banner__text {
    max-width: 700px;
    width: 100%;
    box-sizing: border-box;
  }

  .hero-banner__heading {
    font-family: var(--font-heading);
    font-size: clamp(38px, 5.5vw, 72px);
    font-weight: 700;
    color: var(--white);
    margin-bottom: var(--spacing-md);
    line-height: 1.2;
    letter-spacing: -0.02em;
    text-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    word-wrap: break-word;
    overflow-wrap: break-word;
    hyphens: auto;
    padding: 0;
  }

  .hero-banner__heading-line {
    display: block;
    background: linear-gradient(135deg, var(--white) 0%, var(--sage-green) 50%, var(--white) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    background-size: 200% auto;
    animation: slideInLeft 1s cubic-bezier(0.16, 1, 0.3, 1) 0.2s both, shimmer 3s infinite;
    filter: drop-shadow(0 2px 10px rgba(74, 124, 44, 0.4));
    word-wrap: break-word;
    overflow-wrap: break-word;
  }

  .hero-banner__subheading {
    font-size: clamp(18px, 2.2vw, 24px);
    font-weight: 400;
    color: rgba(255, 255, 255, 0.95);
    margin-bottom: var(--spacing-lg);
    line-height: 1.7;
    max-width: 600px;
    width: 100%;
    animation: fadeInUp 1s cubic-bezier(0.16, 1, 0.3, 1) 0.4s both;
    word-wrap: break-word;
    overflow-wrap: break-word;
  }

  .hero-banner__trust-badges {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-xl);
    flex-wrap: wrap;
    animation: fadeInUp 1s cubic-bezier(0.16, 1, 0.3, 1) 0.5s both;
    width: 100%;
  }

  .trust-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-xs);
    padding: var(--spacing-xs) var(--spacing-md);
    background-color: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: var(--radius-full);
    color: var(--white);
    font-size: var(--body-small);
    font-weight: 600;
    white-space: nowrap;
  }

  .trust-badge svg {
    flex-shrink: 0;
  }

  .hero-banner__ctas {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    flex-wrap: wrap;
    animation: fadeInUp 1s cubic-bezier(0.16, 1, 0.3, 1) 0.6s both;
    width: 100%;
  }

  .hero-banner__cta-primary {
    background: linear-gradient(135deg, var(--forest-green) 0%, var(--sage-green) 50%, var(--forest-green) 100%);
    background-size: 200% auto;
    color: var(--white);
    border: none;
    padding: var(--spacing-md) var(--spacing-2xl);
    font-weight: 600;
    font-size: clamp(15px, 1.4vw, 18px);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-sm);
    box-shadow: 0 8px 30px rgba(74, 124, 44, 0.5), 0 0 0 0 rgba(74, 124, 44, 0.3);
    position: relative;
    overflow: hidden;
    text-transform: none;
    letter-spacing: 0.02em;
    animation: pulse-glow 2s ease-in-out infinite;
    white-space: nowrap;
    max-width: 100%;
    box-sizing: border-box;
  }

  .hero-banner__cta-primary::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    transition: left 0.5s;
  }

  .hero-banner__cta-primary:hover::before {
    left: 100%;
  }

  .hero-banner__cta-primary:hover {
    color: var(--white);
    transform: translateY(-4px) scale(1.02);
    background-position: 100% center;
    box-shadow: 0 12px 40px rgba(74, 124, 44, 0.6);
  }

  .hero-banner__cta-primary svg {
    transition: transform var(--transition-base);
  }

  .hero-banner__cta-primary:hover svg {
    transform: translateX(4px);
  }

  .hero-banner__cta-secondary {
    background-color: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    color: var(--white);
    border: 2px solid rgba(255, 255, 255, 0.3);
    padding: var(--spacing-md) var(--spacing-2xl);
    white-space: nowrap;
    max-width: 100%;
    box-sizing: border-box;
  }

  .hero-banner__cta-secondary:hover {
    background-color: rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.5);
    transform: translateY(-2px);
  }

  .hero-banner__scroll-indicator {
    position: absolute;
    bottom: var(--spacing-xl);
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-xs);
    color: rgba(255, 255, 255, 0.7);
    font-size: var(--body-small);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    animation: fadeInUp 1s cubic-bezier(0.16, 1, 0.3, 1) 0.8s both, bounce 2s infinite 2s;
    z-index: 4;
    pointer-events: none;
  }

  .hero-banner__scroll-indicator svg {
    animation: bounceArrow 1.5s infinite;
  }

  .hero-banner__carousel-controls {
    position: absolute;
    bottom: var(--spacing-xl);
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    z-index: 4;
  }

  .hero-banner__carousel-prev,
  .hero-banner__carousel-next {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    border: 2px solid rgba(255, 255, 255, 0.5);
    background: rgba(255, 255, 255, 0.15);
    color: var(--white);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.2s, border-color 0.2s, transform 0.2s;
  }

  .hero-banner__carousel-prev:hover,
  .hero-banner__carousel-next:hover {
    background: rgba(255, 255, 255, 0.25);
    border-color: rgba(255, 255, 255, 0.8);
    transform: scale(1.05);
  }

  .hero-banner__carousel-prev:focus-visible,
  .hero-banner__carousel-next:focus-visible {
    outline: 2px solid var(--white);
    outline-offset: 2px;
  }

  .hero-banner__carousel-dots {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
  }

  .hero-banner__carousel-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    border: 2px solid rgba(255, 255, 255, 0.6);
    background: transparent;
    cursor: pointer;
    padding: 0;
    transition: background 0.2s, transform 0.2s;
  }

  .hero-banner__carousel-dot:hover {
    background: rgba(255, 255, 255, 0.4);
  }

  .hero-banner__carousel-dot--active {
    background: var(--white);
    transform: scale(1.2);
  }

  .hero-banner__carousel-dot:focus-visible {
    outline: 2px solid var(--white);
    outline-offset: 2px;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateX(-50%) translateY(0);
    }
    40% {
      transform: translateX(-50%) translateY(-10px);
    }
    60% {
      transform: translateX(-50%) translateY(-5px);
    }
  }

  @keyframes bounceArrow {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(8px);
    }
  }

  @keyframes shimmer {
    0% {
      background-position: 0% center;
    }
    100% {
      background-position: 200% center;
    }
  }

  @keyframes float {
    0%, 100% {
      transform: translate(0, 0) scale(1);
    }
    50% {
      transform: translate(15px, -15px) scale(1.03);
    }
  }

  @keyframes pulse-glow {
    0%, 100% {
      box-shadow: 0 8px 30px rgba(74, 124, 44, 0.5), 0 0 0 0 rgba(74, 124, 44, 0.3);
    }
    50% {
      box-shadow: 0 8px 40px rgba(74, 124, 44, 0.6), 0 0 0 10px rgba(74, 124, 44, 0);
    }
  }

  @media (max-width: 768px) {
    .hero-banner {
      height: 85vh;
      min-height: 500px;
      max-height: 700px;
    }

    .hero-banner__content {
      padding: var(--spacing-xl) var(--spacing-md);
      text-align: center;
    }

    .hero-banner__text {
      max-width: 100%;
      width: 100%;
    }

    .hero-banner__heading {
      font-size: clamp(32px, 8vw, 48px);
      margin-bottom: var(--spacing-sm);
      padding: 0;
    }

    .hero-banner__subheading {
      font-size: clamp(16px, 4vw, 20px);
      margin-bottom: var(--spacing-lg);
      line-height: 1.6;
    }

    .hero-banner__trust-badges {
      gap: var(--spacing-xs);
      margin-bottom: var(--spacing-xl);
      flex-wrap: wrap;
      justify-content: center;
    }

    .trust-badge {
      font-size: var(--body-xs);
      padding: var(--spacing-xs) var(--spacing-sm);
      min-height: 32px;
      display: flex;
      align-items: center;
    }

    .hero-banner__ctas {
      flex-direction: column;
      width: 100%;
      gap: var(--spacing-md);
    }

    .hero-banner__ctas .btn {
      width: 100%;
      min-height: 52px;
      justify-content: center;
      padding: var(--spacing-md) var(--spacing-xl);
      font-size: var(--body-large);
      font-weight: 700;
      -webkit-tap-highlight-color: transparent;
    }

    .hero-banner__cta-primary {
      box-shadow: 0 4px 15px rgba(74, 124, 44, 0.4);
    }

    .hero-banner__scroll-indicator {
      display: none;
    }

    .hero-banner__carousel-controls {
      bottom: var(--spacing-lg);
    }

    .hero-banner__carousel-prev,
    .hero-banner__carousel-next {
      width: 44px;
      height: 44px;
    }

    /* Reduce animations on mobile */
    .hero-banner__overlay::before,
    .hero-banner__overlay::after {
      display: none;
    }

    .hero-banner__gradient-ornament {
      display: none;
    }

    .hero-banner__cta-primary {
      animation: none;
    }
  }

  @media (max-width: 480px) {
    .hero-banner {
      height: 80vh;
      min-height: 450px;
      max-height: 600px;
    }

    .hero-banner__content {
      padding: var(--spacing-lg) var(--spacing-sm);
    }

    .hero-banner__heading {
      font-size: clamp(28px, 7vw, 40px);
      margin-bottom: var(--spacing-sm);
    }

    .hero-banner__subheading {
      font-size: clamp(14px, 3.5vw, 18px);
      margin-bottom: var(--spacing-md);
    }

    .hero-banner__ctas .btn {
      min-height: 48px;
      padding: var(--spacing-sm) var(--spacing-lg);
      font-size: var(--body-normal);
    }

    .trust-badge {
      font-size: 10px;
      padding: var(--spacing-xs);
    }
  }
{% endstylesheet %}

<script>
  (function () {
    const section = document.getElementById('hero-banner-{{ section.id }}');
    if (!section || !section.classList.contains('hero-banner--carousel')) return;

    const slides = Array.from(section.querySelectorAll('.hero-banner__slide:not(.hero-banner__slide--empty)'));
    const dots = Array.from(section.querySelectorAll('.hero-banner__carousel-dot'));
    const btnPrev = section.querySelector('[data-action="prev"]');
    const btnNext = section.querySelector('[data-action="next"]');

    if (slides.length <= 1) return;

    const speed = parseInt(section.dataset.autoplaySpeed, 10) || 5000;
    const duration = parseInt(section.dataset.transitionDuration, 10) || 800;
    section.style.setProperty('--hero-transition-duration', duration + 'ms');

    let currentIndex = 0;
    let timer = null;

    function goToSlide(index) {
      index = ((index % slides.length) + slides.length) % slides.length;
      currentIndex = index;
      slides.forEach(function (slide, i) {
        slide.classList.toggle('hero-banner__slide--active', i === index);
      });
      dots.forEach(function (dot, i) {
        dot.classList.toggle('hero-banner__carousel-dot--active', i === index);
        dot.setAttribute('aria-selected', i === index);
      });
    }

    function next() { goToSlide(currentIndex + 1); }
    function prev() { goToSlide(currentIndex - 1); }

    function startAutoplay() {
      if (timer) clearInterval(timer);
      if (section.dataset.paused === 'true') return;
      timer = setInterval(next, speed);
    }
    function stopAutoplay() {
      if (timer) { clearInterval(timer); timer = null; }
    }

    function setPaused(paused) {
      section.dataset.paused = paused ? 'true' : 'false';
      if (paused) stopAutoplay();
      else startAutoplay();
    }

    section.addEventListener('mouseenter', function () { setPaused(true); });
    section.addEventListener('mouseleave', function () { setPaused(false); });
    section.addEventListener('focusin', function () { setPaused(true); });
    section.addEventListener('focusout', function () {
      if (!section.contains(document.activeElement)) setPaused(false);
    });

    document.addEventListener('keydown', function (e) {
      if (!section.contains(document.activeElement)) return;
      if (e.key === 'ArrowLeft') { e.preventDefault(); prev(); }
      else if (e.key === 'ArrowRight') { e.preventDefault(); next(); }
    });

    if (btnPrev) btnPrev.addEventListener('click', function () { prev(); startAutoplay(); });
    if (btnNext) btnNext.addEventListener('click', function () { next(); startAutoplay(); });

    dots.forEach(function (dot, i) {
      dot.addEventListener('click', function () { goToSlide(i); startAutoplay(); });
      dot.addEventListener('keydown', function (e) {
        if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); goToSlide(i); startAutoplay(); }
      });
    });

    startAutoplay();
  })();
</script>

{% schema %}
{
  "name": "Hero Banner",
  "settings": [
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero image (fallback when no slides)"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Pure Nutrition from Nature"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Premium fruit & vegetable powders for your wellness journey"
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "Primary CTA Text",
      "default": "Shop All Products"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "Primary CTA Link"
    },
    {
      "type": "text",
      "id": "secondary_cta_text",
      "label": "Secondary CTA Text",
      "default": "Learn About Us"
    },
    {
      "type": "url",
      "id": "secondary_cta_link",
      "label": "Secondary CTA Link"
    },
    {
      "type": "header",
      "content": "Background carousel"
    },
    {
      "type": "range",
      "id": "carousel_speed",
      "label": "Auto-play speed (seconds)",
      "min": 3,
      "max": 10,
      "step": 1,
      "default": 5,
      "unit": "s"
    },
    {
      "type": "range",
      "id": "transition_duration",
      "label": "Transition duration (ms)",
      "min": 400,
      "max": 1500,
      "step": 100,
      "default": 800,
      "unit": "ms"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay opacity (text readability)",
      "min": 0,
      "max": 100,
      "step": 5,
      "default": 85,
      "unit": "%"
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "limit": 10,
      "settings": [
        {
          "type": "paragraph",
          "content": "Upload or select an image for this slide. It will appear as the background."
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Slide image"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Hero Banner",
      "blocks": [
        { "type": "slide" },
        { "type": "slide" },
        { "type": "slide" }
      ]
    }
  ]
}
{% endschema %}

